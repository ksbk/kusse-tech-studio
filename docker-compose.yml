# DEPRECATED: This file will be removed in a future version
# Please use the explicit commands below for better clarity and control
#
# Development:
#   docker-compose -f infra/docker-compose.base.yml -f infra/docker-compose.development.yml up
#
# Staging:
#   docker-compose -f infra/docker-compose.base.yml -f infra/docker-compose.staging.yml up -d
#
# Production:
#   docker-compose -f infra/docker-compose.base.yml -f infra/docker-compose.production.yml up -d

# Minimal root Docker Compose file for KusseTechStudio
# Acts as a thin wrapper to the organized infra/ configurations

include:
  - infra/docker-compose.base.yml
  - infra/docker-compose.override.yml
# For other environments, use:
# docker-compose -f infra/docker-compose.base.yml -f infra/docker-compose.staging.yml up -d
# docker-compose -f infra/docker-compose.base.yml -f infra/docker-compose.production.yml up -d
